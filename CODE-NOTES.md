# Заметки по коду (todo)

- [ ] **Пустая комната при удалении последнего вложенного** — разобрать и зафиксировать поведение: если у `Room` не остаётся ни одного «содержимого» (ни зарегистрированных `RoomMember`, ни вложенных детей-комнат / релевантного дерева — уточнить в ТЗ), нужно ли **автоматически удалять** GameObject с `Room` при удалении объектов со сцены / из иерархии. Сейчас эта логика **не реализована** (откат). При повторной реализации: editor-hook, согласованность с `Destroy`/`DestroyImmediate` в тестах, каскад пустых родителей.
- [x] **Shift: растягивание блоков вдоль нормалей к плоскости** — реализовать режим (с зажатым Shift): при манипуляции выбранными **building block**-ами растягивать их **вдоль нормалей** к **текущей выбранной плоскости** (ось масштаба/экструзии согласовать с гизмо/скетч-плоскостью; уточнить в UX).
- [ ] **OneSideScale: clamp при сжатии** — при уменьшении по плоскости не перемещать объект дальше вдоль оси уменьшения; нужен блок: если размер достиг минимального порога, дальнейшие шаги игнорируем (и не двигаем `Transform.position`).
- [ ] (добавляйте сюда пункты по мере появления)

## Если ассет долго грузится (Editor) — что оптимизируем

- [ ] **Сначала измеряем, что реально тормозит**: включаем/используем `SketchKitInitTimings` (см. `Assets/3DSketchKit/Editor/Diagnostics/SketchKitInitTimings.cs`) и сравниваем:
  - `Unity startup time` (общее время старта редактора)
  - `Sketch Kit init time` (окно инициализации ассета + разбивка по модулям)
- [ ] **Подсветка граней без тиков?** Вопрос: нужно ли всегда держать подсветку граней “живой” в `SceneView` (каждый кадр при зажатом Shift), или достаточно отрисовывать/обновлять только пока Shift зажат и ЛКМ не нажата, а после отпускания ЛКМ/Shift пересчитывать заново только при новом нажатии Shift (снижение нагрузки за счёт меньшего количества перерисовок).
- [ ] **Убираем тяжёлые операции из автозапуска**: всё, что висит на `[InitializeOnLoad]`, переводим на “ленивую” инициализацию (по открытию окна `Window > 3D Sketch Kit` или по кнопке).
- [ ] **Reflection-скан типов** (`AbilityTypeCatalog.RefreshDiscoveredAbilities`):
  - в Editor заменить `AppDomain.CurrentDomain.GetAssemblies()+GetTypes()` на `UnityEditor.TypeCache` (`TypeCache.GetTypesWithAttribute<SketchKitAbilityIdAttribute>()`) и фильтрацию по `IAbility`
  - добавить кэш (например, в `SessionState`/`EditorPrefs`) и инвалидировать по событию компиляции/перезагрузки домена
- [ ] **Избегаем лишних подписок/хуков SceneView**: всё, что реагирует на `SceneView.duringSceneGui`, должно быть максимально лёгким, а тяжёлые расчёты выполняться только при реальном использовании инструмента (например, при зажатом Shift и наличии hover).

## Не реализовано по ТЗ (MVP) — не считать багами

- [ ] **Документация**: `Assets/3DSketchKit/Documentation/` — только плейсхолдер, PDF/HTML из ТЗ не добавлены.
- [ ] **Префабы**: `Assets/3DSketchKit/Prefabs/*` — по ТЗ должны быть заготовки, сейчас это в основном структура/папки; готовый набор (стены/персонажи/зоны/UI) не поставляется.
- [ ] **Effects-модуль**: `Runtime/Modules/Effects/` — заглушка; отдельные модули эффектов не заведены (кроме минимального хранения в `EffectReceiverComponent`).
- [ ] **AbilityData**: иконка/кулдаун есть в SO, но логика кулдауна и UI в рантайме не реализованы.
- [ ] **ZoneEffectData.Duration**: длительность пока не обрабатывается (нет таймера/истечения по duration).
- [ ] **ZoneEffectData.VisualPrefab**: VFX-префаб не инстанциируется из `ZoneTrigger`.
- [ ] **RoomData.RoomId**: хранится, но почти не используется в логике `Room` (важно только `DestroyRule`).
