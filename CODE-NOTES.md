# Заметки по коду (todo)

- [ ] **Пустая комната при удалении последнего вложенного** — разобрать и зафиксировать поведение: если у `Room` не остаётся ни одного «содержимого» (ни зарегистрированных `RoomMember`, ни вложенных детей-комнат / релевантного дерева — уточнить в ТЗ), нужно ли **автоматически удалять** GameObject с `Room` при удалении объектов со сцены / из иерархии. Сейчас эта логика **не реализована** (откат). При повторной реализации: editor-hook, согласованность с `Destroy`/`DestroyImmediate` в тестах, каскад пустых родителей.
- [ ] **Shift: растягивание блоков вдоль нормалей к плоскости** — реализовать режим (с зажатым Shift): при манипуляции выбранными **building block**-ами растягивать их **вдоль нормалей** к **текущей выбранной плоскости** (ось масштаба/экструзии согласовать с гизмо/скетч-плоскостью; уточнить в UX).
- [ ] (добавляйте сюда пункты по мере появления)

## Не реализовано по ТЗ (MVP) — не считать багами

- [ ] **Документация**: `Assets/3DSketchKit/Documentation/` — только плейсхолдер, PDF/HTML из ТЗ не добавлены.
- [ ] **Префабы**: `Assets/3DSketchKit/Prefabs/*` — по ТЗ должны быть заготовки, сейчас это в основном структура/папки; готовый набор (стены/персонажи/зоны/UI) не поставляется.
- [ ] **Effects-модуль**: `Runtime/Modules/Effects/` — заглушка; отдельные модули эффектов не заведены (кроме минимального хранения в `EffectReceiverComponent`).
- [ ] **AbilityData**: иконка/кулдаун есть в SO, но логика кулдауна и UI в рантайме не реализованы.
- [ ] **ZoneEffectData.Duration**: длительность пока не обрабатывается (нет таймера/истечения по duration).
- [ ] **ZoneEffectData.VisualPrefab**: VFX-префаб не инстанциируется из `ZoneTrigger`.
- [ ] **RoomData.RoomId**: хранится, но почти не используется в логике `Room` (важно только `DestroyRule`).
